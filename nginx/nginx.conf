user nginx;
worker_processes auto;
error_log /var/log/nginx/error.log warn;
pid /var/run/nginx.pid;

events {
    worker_connections 1024;
}

http {
    # warn/error entries echo the raw request line (bearer URLs); crit and
    # above keep worker and file-descriptor failures visible. Upstream 502s
    # remain visible as statuses in the safe access log.
    error_log /var/log/nginx/error.log crit;
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    # URLs and Referer can contain gallery, image and customer bearer tokens.
    map $uri $request_surface {
        ~*^/api/(?<picpeak_surface>[a-z-]+)(?:/|$) /api/$picpeak_surface;
        default /;
    }
    log_format picpeak_safe '$remote_addr "$http_x_forwarded_for" [$time_local] "$request_method $request_surface" '
                            '$status $body_bytes_sent $request_time';
    access_log /var/log/nginx/access.log picpeak_safe;

    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 65;
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/javascript application/xml+rss application/json;

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "no-referrer-when-downgrade" always;

    # Rate limiting
    limit_req_zone $binary_remote_addr zone=general:10m rate=10r/s;
    limit_req_zone $binary_remote_addr zone=auth:10m rate=5r/m;

    include /etc/nginx/sites-enabled/*.conf;
}
